namespace Hexalith.ChatBot.Server.Lifecycle.Workflows;

/// <summary>ChatBot-owned ordering taxonomy for source records in an ingestion binding.</summary>
internal enum IngestionBindingRecordKind
{
    Message,
    Attachment,
}
