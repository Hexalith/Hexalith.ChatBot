---
scope: Epic 12 Story 12.15 recovery-primary provenance lifecycle
purpose: build substrate and implementation/reviewer sign-off
altitude: epic
updated: 2026-09-14T09:18
---

- (constraint) Existing TE-2 policy v2 masks only scope.implementationDigest during canonical hashing; retained provenance sidecars are immutable and CI attestation never mints or replaces them.
- (constraint) Story 12.15 recovery-primary is the logical primary lane bound to the live LiveContinuityAspireE2eTests class; scheduled and release recovery workflows currently emit TRX plus domain manifests but no TE-2 provenance sidecar.
- (decision by user) Use the recommended transition-declared current-run producer design; do not design a producer-side retained-attestation lifecycle in this pass.
- (version) GitHub Actions documentation checked 2026-08-24: jobs default to 360 minutes; github.run_id is stable across reruns while github.run_attempt increments; upload-artifact v4 artifacts are immutable and receive their artifact ID only after upload.
- (decision) Bind recovery-primary completion evidence to current-run only. The active transition contract uses file:<trx-relative-path>; story-evidence-integrity conditionally runs the live class, preflights all current-run lanes, mints the sidecar with the exact scope digest, then validates and uploads reports/sidecars.
- (decision) Scheduled/release live-recovery artifacts remain operational/A10 evidence evaluated by the independent recovery evidence gate; they are not story-completion evidence and do not satisfy recovery-primary.
- (decision) The story-evidence job retains read-only contents/actions permissions, runs recovery only when an active transition contract declares current-run recovery-primary, and fails closed on producer, DAPR cleanup, attestation, or validation failure.
- (assumption) The 360-minute job budget is sufficient for existing preflight plus the recovery lane whose configured runner budget is 330 minutes; hosted execution remains required to confirm operational headroom.
- (gap by Winston) Reviewer gate found contract/producer path-cardinality drift, raw jq side-effect authorization, unchecked raw-TRX publication, floating Dapr runtime, imprecise artifact immutability, and missing cleanup reserve.
- (decision by Winston) Pin recovery-primary to one active consumer, canonical TRX/provenance paths and locator; authorize only from repository-owned side-effect-free production plan.
- (decision by Winston) Stage raw recovery TRX outside all upload paths; RecoveryTrxSanitizer exclusively projects the bound passing test into metadata-only completion evidence; completion operational side-products remain unpublished diagnostics.
- (decision by Winston) Pin Dapr CLI/runtime 1.18.0 with release-asset SHA-256; use artifact upload v7/download v8 on completion path; enforce 40/10/280/30-minute job budget and 265-minute in-process deadline.
- (event by Winston) All three reviewer-lens high findings incorporated; rerun verification started before final Winston sign-off.
- (event by Winston) Reviewer Gate finalized after remediation: rubric PASS; reality/provenance PASS with every completion-consumed producer pinned to Dapr CLI/runtime 1.18.0 and current artifact-action majors plus a dynamically enforced minute-330 closeout deadline; adversarial ADV1-ADV5 PASS. Deterministic spine lint, actionlint, shell/JSON/diff checks, StoryEvidenceGate 182/182, integration timeout contract 16/16, focused architecture contract 1/1, and live-recovery architecture 9/9 passed. Winston architecture sign-off remains scoped to non-circular provenance; hosted Tier-3 authenticity and A10 ratification remain Murat-owned and outstanding.
- (constraint) The 2026-09-14 update has one deliverable: the Epic 12 architecture spine; no additional audience-facing rendering is requested.
- (decision) Bind the transition gate to a unique story-transition-evidence-integrity job identity within the existing CI workflow, running only for pull_request/push transition contexts; use a distinct identity for scheduled/manual diagnostics. The transition job must be a required GitHub Actions check on protected main before the control is described as active, owned by Amelia/Murat with Winston reviewing the CI boundary. Binds CI identity and repository enforcement; prevents a green no-transition diagnostic check from satisfying or being confused with the story-completion transition gate.
- (decision) Retain completion-run operational output as a separately named recovery-primary-diagnostics artifact with a closed metadata-only schema, 30-day success-or-failure retention, and no TE-2 or A10 authority. It may contain allow-listed scenario manifests/reports, cleanup receipt/status, and a bounded failure summary, but never the canonical completion TRX or TE-2 provenance sidecar; authoritative story-evidence-integrity-reports remain post-validation only. Binds diagnostic and authoritative artifact ownership; prevents pre-validation material from being mistaken for completion or A10 evidence while preserving long-run failure diagnostics.
- (version) Dapr CLI 1.18.2 and runtime 1.18.4 were verified current on 2026-09-14 from official releases; the v1.18.2 Linux amd64 CLI release asset SHA-256 is ccfff008fd16f50096a9192ad56697ac7052e3add6fa0a07789d87b4c4df8c40.
- (decision) Adopt Dapr CLI 1.18.2 with runtime 1.18.4 for recovery-primary, with the verified CLI archive digest and focused recovery/provenance verification as an activation condition. Binds the completion producer runtime baseline; prevents evidence from running on the stale 1.18.0 runtime that predates relevant security and workflow-recovery fixes.
- (constraint) The repository root global.json is the sole .NET SDK version authority: feature band 10.0.400 with latestPatch; CI must derive or assert agreement rather than carry an independent 10.0.302 declaration.
- (version) Microsoft .NET 10 release metadata checked 2026-09-14: global.json feature band 10.0.400 with latestPatch resolves locally and in current release metadata to supported SDK 10.0.401; the exact servicing patch is intentionally selected by that policy rather than duplicated in the spine.
- (constraint) The live producer's Aspire authority is the AppHost SDK and aligned central package catalog, currently 13.5.3; the spine and companions must describe that authority and not retain the superseded 13.4.6 literal.
- (version) Aspire 13.5.3 was verified current from the official release on 2026-09-14 and matches the AppHost SDK.
