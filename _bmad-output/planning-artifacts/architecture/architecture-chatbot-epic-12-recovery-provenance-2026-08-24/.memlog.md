---
scope: Epic 12 Story 12.15 recovery-primary provenance lifecycle
purpose: build substrate and implementation/reviewer sign-off
altitude: epic
updated: 2026-09-14T10:11
---

- (constraint) Existing TE-2 policy v2 masks only scope.implementationDigest during canonical hashing; retained provenance sidecars are immutable and CI attestation never mints or replaces them.
- (constraint) Story 12.15 recovery-primary is the logical primary lane bound to the live LiveContinuityAspireE2eTests class; scheduled and release recovery workflows currently emit TRX plus domain manifests but no TE-2 provenance sidecar.
- (decision by user) Use the recommended transition-declared current-run producer design; do not design a producer-side retained-attestation lifecycle in this pass.
- (version) GitHub Actions documentation checked 2026-08-24: jobs default to 360 minutes; github.run_id is stable across reruns while github.run_attempt increments; upload-artifact v4 artifacts are immutable and receive their artifact ID only after upload.
- (decision) Bind recovery-primary completion evidence to current-run only. The active transition contract uses file:<trx-relative-path>; story-evidence-integrity conditionally runs the live class, preflights all current-run lanes, mints the sidecar with the exact scope digest, then validates and uploads reports/sidecars.
- (decision) Scheduled/release live-recovery artifacts remain operational/A10 evidence evaluated by the independent recovery evidence gate; they are not story-completion evidence and do not satisfy recovery-primary.
- (decision) The story-evidence job retains read-only contents/actions permissions, runs recovery only when an active transition contract declares current-run recovery-primary, and fails closed on producer, DAPR cleanup, attestation, or validation failure.
- (assumption) The 360-minute job budget is sufficient for existing preflight plus the recovery lane whose configured runner budget is 330 minutes; hosted execution remains required to confirm operational headroom.
- (gap by Winston) Reviewer gate found contract/producer path-cardinality drift, raw jq side-effect authorization, unchecked raw-TRX publication, floating Dapr runtime, imprecise artifact immutability, and missing cleanup reserve.
- (decision by Winston) Pin recovery-primary to one active consumer, canonical TRX/provenance paths and locator; authorize only from repository-owned side-effect-free production plan.
- (decision by Winston) Stage raw recovery TRX outside all upload paths; RecoveryTrxSanitizer exclusively projects the bound passing test into metadata-only completion evidence; completion operational side-products remain unpublished diagnostics.
- (decision by Winston) Pin Dapr CLI/runtime 1.18.0 with release-asset SHA-256; use artifact upload v7/download v8 on completion path; enforce 40/10/280/30-minute job budget and 265-minute in-process deadline.
- (event by Winston) All three reviewer-lens high findings incorporated; rerun verification started before final Winston sign-off.
- (event by Winston) Reviewer Gate finalized after remediation: rubric PASS; reality/provenance PASS with every completion-consumed producer pinned to Dapr CLI/runtime 1.18.0 and current artifact-action majors plus a dynamically enforced minute-330 closeout deadline; adversarial ADV1-ADV5 PASS. Deterministic spine lint, actionlint, shell/JSON/diff checks, StoryEvidenceGate 182/182, integration timeout contract 16/16, focused architecture contract 1/1, and live-recovery architecture 9/9 passed. Winston architecture sign-off remains scoped to non-circular provenance; hosted Tier-3 authenticity and A10 ratification remain Murat-owned and outstanding.
- (constraint) The 2026-09-14 update has one deliverable: the Epic 12 architecture spine; no additional audience-facing rendering is requested.
- (decision) Bind the transition gate to a unique story-transition-evidence-integrity job identity within the existing CI workflow, running only for pull_request/push transition contexts; use a distinct identity for scheduled/manual diagnostics. The transition job must be a required GitHub Actions check on protected main before the control is described as active, owned by Amelia/Murat with Winston reviewing the CI boundary. Binds CI identity and repository enforcement; prevents a green no-transition diagnostic check from satisfying or being confused with the story-completion transition gate.
- (decision) Retain completion-run operational output as a separately named recovery-primary-diagnostics artifact with a closed metadata-only schema, 30-day success-or-failure retention, and no TE-2 or A10 authority. It may contain allow-listed scenario manifests/reports, cleanup receipt/status, and a bounded failure summary, but never the canonical completion TRX or TE-2 provenance sidecar; authoritative story-evidence-integrity-reports remain post-validation only. Binds diagnostic and authoritative artifact ownership; prevents pre-validation material from being mistaken for completion or A10 evidence while preserving long-run failure diagnostics.
- (version) Dapr CLI 1.18.2 and runtime 1.18.4 were verified current on 2026-09-14 from official releases; the v1.18.2 Linux amd64 CLI release asset SHA-256 is ccfff008fd16f50096a9192ad56697ac7052e3add6fa0a07789d87b4c4df8c40.
- (decision) Adopt Dapr CLI 1.18.2 with runtime 1.18.4 for recovery-primary, with the verified CLI archive digest and focused recovery/provenance verification as an activation condition. Binds the completion producer runtime baseline; prevents evidence from running on the stale 1.18.0 runtime that predates relevant security and workflow-recovery fixes.
- (constraint) The repository root global.json is the sole .NET SDK version authority: feature band 10.0.400 with latestPatch; CI must derive or assert agreement rather than carry an independent 10.0.302 declaration.
- (version) Microsoft .NET 10 release metadata checked 2026-09-14: global.json feature band 10.0.400 with latestPatch resolves locally and in current release metadata to supported SDK 10.0.401; the exact servicing patch is intentionally selected by that policy rather than duplicated in the spine.
- (constraint) The live producer's Aspire authority is the AppHost SDK and aligned central package catalog, currently 13.5.3; the spine and companions must describe that authority and not retain the superseded 13.4.6 literal.
- (version) Aspire 13.5.3 was verified current from the official release on 2026-09-14 and matches the AppHost SDK.
- (decision) Allocate the final 30-minute reserve by absolute deadlines derived from the recorded job start: producer hard stop at minute 330; Dapr/topology teardown complete or failed by minute 335; cleanup receipt evaluation, projection, attestation, and validation complete or failed by minute 350; minutes 350-360 are a non-consumable publication floor. Every phase derives its remaining timeout from these deadlines; teardown timeout records bounded failure metadata and blocks successful attestation. Binds closeout phase ownership; prevents teardown or validation from consuming the only publication window.
- (decision) Ratify the implemented timeout ladder as 250 minutes in-process, external SIGINT no later than 265 minutes with a 15-minute kill-after, 285-minute producer step, fixed minute-330 producer closeout, and 360-minute job ceiling; Dapr admission remains before minute 40 and initialization remains capped at 10 minutes. CI owns the outer values, the recovery harness owns its in-process deadline within that envelope, and architecture tests must enforce 250 < 265 < 285 < 330 < 360. Binds timeout ownership and ordering; prevents a maintainer from collapsing the in-process cleanup opportunity or consuming closeout reserve.
- (decision) Make one aggregate recovery cleanup receipt a mandatory current-run input before sanitization or attestation. Scenario producers register all run-owned mutations and emit closed metadata-only cleanup observations; a repository-owned post-producer finalizer is the sole receipt writer after injected faults are restored, compensating erasures finish, a bounded quiescence window and postcondition reads pass, the Aspire application is disposed, and Dapr/ephemeral backing resources are torn down. The mutation set covers injected resource/subscription fault state, run-owned derived/read-model/sentinel/fresh-partition keys, and ephemeral topology state; immutable EventStore history is never directly deleted and must remain run-scoped until ephemeral-store teardown. RecoveryTrxSanitizer must reject any missing, false, incomplete, or timed-out receipt and bind the complete receipt digest/status into the canonical TRX before ProvenanceAttestor runs; failure is retained only as bounded diagnostic metadata and keeps the transition check red. Binds cleanup ownership and proof; prevents a passing TRX from being attested while destructive test state or a fault remains.
- (version) GitHub action refs verified 2026-09-14: actions/upload-artifact v7 resolves to v7.0.1 commit 043fb46d1a93c77aae656e7c1c64a875d1fc6a0a; actions/download-artifact v8 resolves to v8.0.1 commit 3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c.
- (decision) Pin completion-path upload-artifact and download-artifact to their reviewed full commit SHAs, retaining release-version comments and accepting updates only through reviewed dependency changes. Binds executable artifact transport code; prevents mutable major tags from changing the completion trust boundary beneath an exact-head run.
- (direction) Carry DW-124 explicitly under Deferred: contract-authored lane names can obtain the 360-minute freshness override outside exact CI planning. Owner is the ChatBot story-evidence gate; revisit when every age-overridden lane is allowlisted or binding-verified regardless of requiredClasses, proven by refusal of recovery-primary without its corresponding trigger.
- (constraint) The updated spine is authoritative for recovery-primary provenance decisions, but its new transition identity/enforcement, diagnostic split, Dapr patch pairing, cleanup receipt, immutable action references, and companion-document reconciliation are activation work; the spine must state that current repository conformance is pending rather than claim these controls are already active.
- (decision) Define recovery-cleanup-policy.json v1 as the repository-owned expected observation set and hexalith.chatbot.recovery-cleanup-receipt/v1 as the aggregate cross-process receipt at recovery-primary/recovery-cleanup-receipt.json. The finalizer requires exactly one observation for every policy-declared scenario/mutation class plus Aspire disposal, Dapr uninstall, and ephemeral-store teardown, rejects missing/duplicate/unexpected observations, and matches repository, exact base/head, implementation digest, producer run ID, workflow run ID/attempt, and job identity. Receipt status is the closed set complete, incomplete, timed-out. RecoveryTrxSanitizer hashes the validated exact UTF-8 receipt bytes with SHA-256 and binds that digest/status into the canonical TRX, avoiding competing JSON canonicalization.
- (decision) Use a distinct hexalith.chatbot.recovery-primary-diagnostics/v1 manifest for the non-authoritative diagnostic artifact. Its closed locators name only reports, cleanup/recovery-cleanup-receipt.json, and failure/recovery-attempt-summary.json under artifact:recovery-primary-diagnostics; it has no test-output locator and rejects any TRX or provenance-sidecar entry. Binds diagnostic inventory and locators; prevents current operational manifests from retaining a dangling required results.trx reference after the artifact split.
- (decision) Clarify the timeout origins: 250-minute in-process, 265-minute external-signal cap, and 285-minute step cap are producer-relative durations; CI also caps SIGINT at absolute job minute 315 so the 15-minute kill-after reaches the absolute minute-330 producer hard stop, followed by absolute 335 teardown, 350 finalization, and 360 job deadlines.
- (constraint) TE-2 and Story 12.15 remain review and the updated control remains activation-pending. Frontmatter status final means the architecture decisions are settled, not that the GitHub required check or implementation is active.
- (constraint) Ownership remains split per the TE-2 ledger: Amelia owns workflow/tool implementation, Murat owns evidence and primary-path policy including artifact schemas, and Winston reviews architecture and CI boundaries.
- (decision) Tighten the required identity to the globally unique check-run name story-transition-evidence-integrity (pull_request), emitted only by the pull_request path and always run there; no-transition is an explicit planner verdict, never a skipped job. Push, scheduled, and manual runs use distinct diagnostic identities with no completion authority. Direct protected-main pushes are disallowed; adding merge_group later requires an exact-range architecture update. Binds check name, source, and event; prevents different transition ranges for the same SHA from sharing required-check authority.
- (decision) Define hexalith.chatbot.recovery-cleanup-observation/v1. Every observation carries the same repository, base/head, implementation digest, producer run ID, workflow run ID/attempt, and job identity as the receipt, plus policy-declared scenario, mutation class, stable postcondition IDs, and outcome complete|incomplete|timed-out. The final receipt is complete only when the exact expected set is present and every required postcondition is positively true; mere presence never implies success.
- (decision) Bind cleanup proof into the canonical TRX with exact properties hexalith.cleanupReceipt.schemaVersion, hexalith.cleanupReceipt.status, and hexalith.cleanupReceipt.sha256. StoryEvidenceValidator must load the retained receipt, revalidate identity/schema/status, recompute SHA-256 over its exact UTF-8 bytes, and match all three TRX properties before accepting recovery-primary; the provenance sidecar then binds those canonical TRX bytes.
- (decision) Split the non-consumable publication floor into absolute minute 350-355 for the always-attempted recovery-primary-diagnostics upload and minute 355-360 for the always-attempted story-evidence-integrity-reports upload. Each step derives and enforces its own remaining absolute deadline and uses always-run semantics so failure of the first cannot skip the second.
- (decision) TE-2 in _bmad-output/planning-artifacts/technical-enablers.md is the sole durable activation record. Activation requires it to record the protected branch pattern, exact globally unique check-run name, GitHub Actions source/app, repository ruleset or protection identifier, owners, and a verification run URL/ID; until then activation remains pending.
- (decision) Authorize destructive recovery only on a fresh GitHub-hosted runner with job-local ephemeral topology/state, Testing environment safety guards, generated per-run secrets, and no production endpoints or credentials. Serialize every recovery-primary producer through one repository-scoped concurrency group with cancel-in-progress false; fixed logical test tenants are permitted only inside the isolated topology, and all run-owned record/resource identities bind workflow run ID/attempt. Reusing shared or self-hosted infrastructure requires a new architecture decision. Binds tenant/runner/resource/concurrency isolation; prevents concurrent or misrouted drills from mutating shared state or canceling cleanup.
- (decision) Scope story-transition-evidence-integrity (pull_request) to pull requests whose base ref is protected main. Pull requests to every other base use a distinct diagnostic check identity, so two PRs sharing a head SHA cannot evaluate different bases under the required name.
- (decision) Supersede the repository concurrency-group recommendation: GitHub concurrency cancels an older pending run even when cancel-in-progress is false, which violates the always-run required-check invariant. Because each producer is bound to a fresh runner and fully job-local ephemeral topology/state, recovery-primary jobs may run concurrently without shared mutation. Do not configure a concurrency group on the required job; introducing any shared infrastructure requires a new architecture decision and a non-canceling external lease.
- (decision) RecoveryCleanupTrxBinding in the StoryEvidenceGate tool is the sole serializer/parser authority for cleanup proof in TRX. It writes exactly three unqualified attributes on the TeamTest TestRun root: hexalith.cleanupReceipt.schemaVersion, hexalith.cleanupReceipt.status, and hexalith.cleanupReceipt.sha256; the SHA is 64 lowercase hexadecimal characters. Both RecoveryTrxSanitizer and StoryEvidenceValidator must use this codec, which rejects missing, duplicate, malformed, or unexpected hexalith.cleanupReceipt.* attributes.
- (event) Spine update finalized 2026-09-14 after deterministic lint PASS, rubric PASS, adversarial PASS, and reality PASS WITH ACTIVATION GUARD; no Critical or High findings remain.
