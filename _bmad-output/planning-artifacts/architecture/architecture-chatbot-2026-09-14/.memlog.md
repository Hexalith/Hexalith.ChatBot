---
scope: Hexalith.ChatBot feature architecture and release-gate contracts
purpose: Implementation consistency contract reconciled to the finalized 2026-09-14 PRD and normative appendices
altitude: feature
updated: 2026-09-15T09:39
---

- (constraint) The finalized PRD, approved addendum, source manifest, qualification evidence, and full-sibling A13 reconciliation dated 2026-09-14 are the product authority; architecture may not weaken or silently reinterpret them.
- (decision) Supersede legacy D4: every durable mutation binds domain event, lifetime idempotency result, policy/approval references, and canonical hash-linked audit envelope in one atomic commit; investigation projections are post-commit and cannot repair a missing canonical envelope. Prevents unaudited committed mutations and competing audit sources of truth.
- (decision) All public state mutations from UI, CLI, MCP, service clients, AI actors, workers, and mailbox events enter the one shared CommandGateway/EventStore command spine, with expected revision or an A13-approved owner concurrency guard. Prevents surface, worker, and producer bypasses.
- (decision) Workflow state, transition, terminal/successor, stable operation_id/decision_slot_id, retry, and audit semantics inherit the PRD Shared Workflow Contract and addendum Retry Profiles verbatim; architecture does not create alternate lifecycle vocabularies. Prevents divergent independently implemented state machines.
- (decision) M0 governance state is created only through first-class commands using two distinct current Tenants TenantOwner principals for the first ChatBot admin grant, tenant policy snapshot, and four enumerated M0 service-client grants; direct seeding is forbidden. Prevents an unaudited or unauthorised bootstrap bypass.
- (constraint) A5, A6, A10, A11, and A13 remain open evidence/approval gates. A5/A6/A13 block M0 and M1; A10/A11 additionally block M2. No repository state, interface, historical bundle, or architectural completion may close them.
- (decision) Cross-context source-of-truth ownership follows PRD Context Ownership: Conversations owns messages and conversation-to-Project assignment; Projects owns Project identity/access/lifecycle/authorization; Parties identity; Folders governed files; Tenants tenant facts/boundaries/policies/auth context; EventStore its current command/event gateway; ChatBot only orchestration and enumerated derived records. Prevents dual writes and authority shadowing.
- (constraint) Epic 12 recovery-provenance AD-6 through AD-9 are adopted for evidence-channel separation but remain activation pending. Diagnostics, story-completion evidence, and A10 operational evidence are disjoint; none substitutes for another.
- (decision) A11 operating rows remain qualification backlog entries until numeric targets, error budgets, live signal provenance, alert routing, burn-test evidence, and exact-candidate binding are all present; unsupported is the required state otherwise. Prevents unsupported production-readiness claims.
- (version) Repository pins reality-checked 2026-09-14: .NET SDK 10.0.400/net10.0/C#14; Aspire AppHost 13.5.3; Dapr .NET SDK 1.18.5; Fluent UI Blazor 5.0.0-rc.5-26219.1; ModelContextProtocol 2.2.0; System.CommandLine 2.0.11; xUnit v3 4.0.0. Dependency upgrades remain separately governed.
- (decision) The addendum Tenant Policy Schema is the sole closed policy catalog; unsafe/unknown snapshots reject atomically, row mutators and independent approvals are binding, safety.controls changes only by its commands, and A6-dependent rows have no persistence-enabling default. Prevents policy drift and bootstrap shortcuts.
- (decision) Every ChatBot-derived record class requires A6-approved governance plus native-store/API tenant-isolation proof before first persistence or machine-surface exposure; M2 cannot retroactively legitimize M0/M1 stores. Prevents unqualified sensitive storage and app-filter-only isolation.
- (decision) AssociationScorer, TaskIntentDetector, and ActionRiskClassifier remain independently versioned contracts; AI operation catalog, surface exposure, AI allowlist, and owner executable targets remain distinct deny-by-default artifacts. Prevents semantic kernel coupling and allowlist authority expansion.
- (decision) Replay uses a production-credential-free composition root with effectful adapters replaced, default-deny egress, replay_run_id labeling, production-store/external-ledger invariance fingerprints, and exclusion from production audit completeness. Prevents test execution from affecting production.
- (direction) The System Architect opens a re-check within five business days for material consumed schema, authorization/identifier, integration-topology, or RBAC changes; manifest and memlog are refreshed, inaccessible evidence is blocking, and later content is never silently consumed.
- (decision) The minimum inbound authenticity floor is M0 while outbound authority/draft/send is M1; provider verdict/header/delegation evidence, strict-or-paranoid policy, five fixed outbound authority classes, and execution-time authority revalidation prevent mailbox authority drift.
- (decision) Recovery diagnostics, exact-candidate story-completion authority, and retained operational A10 evidence are disjoint channels; Epic 12 remains activation pending and its preconditions require independent evidence. Prevents evidence laundering between trust purposes.
- (decision) Distinguish every A13 target from current owner capability: compatibility, interfaces, revisions, typed transports, and adopted architecture are non-qualifying until the indivisible exact-candidate owner bundle passes.
- (decision) Cross-context mutations use a durable ChatBot coordinator plus idempotent owner command and owner event/result; the owner aggregate is sole effect/audit transaction owner, with no distributed dual-write.
- (decision) FrontComposer SignalR ProjectionChangedDetail is metadata-only advisory progress transport; authorized query/status state remains authoritative for partial and terminal governed-chat state.
- (constraint) The PRD sole increment table remains complete release authority; A5/A6/A13 block M0/M1 and A10/A11 additionally block M2, with every named gate still open.
- (decision) Final gate ratified EventStore DomainService hosting with CommandGateway as the sole SDK pre-commit hook, local-only AppHost, and OpenAPI 3.1 as the sole HTTP wire authority.
- (decision) Below-application isolation derives physical namespaces from trusted tenant context for keys, cursors, topics, queues, dead letters, search/vector, prompt context, and projections; application predicates alone are prohibited.
- (decision) Runtime controls use one durable fail-closed admission view and tenant-first weighted deficit round-robin scheduling with bounded leases, partitioned poison handling, and one OperationsControlWorker.
- (decision) Replay has no production access; an external gate-owned verifier captures versioned SHA-256 invariance manifests before and after execution.
- (constraint) Current non-qualifying general Dapr CLI/runtime wiring is 1.18.0/1.18.0; accepted Epic 12 recovery-primary target is CLI/runtime 1.18.2/1.18.4 with activation pending. These are distinct version planes.
- (event) spine finalized
- (constraint) The approved 2026-09-15 Sprint Change Proposal is the update authority for both maintained architecture surfaces; the finalized PRD and approved addendum remain normative and unchanged.
- (decision) Amend AD-7: low-risk and approval-required are only successful determinate ActionRiskClassifier classes; any missing, invalid, unqualified, failed, or non-contract artifact/output, missing tag, unknown effect surface, or undeclared authority class returns classifier-indeterminate. It creates no proposal, durable domain or idempotency success, approval action, or effect; only a redacted non-mutating auditable attempt is retained, and remediation requires a new linked operation. Prevents indeterminate requests becoming approvable.
- (decision) Amend AD-9: the exact durable association correction state is CorrectionDelayed, and the frozen impact manifest covers every ChatBot-derived store, affected Conversations/Folders record and index, approved/executed action, appended message, task-intent conversion, sent mail, external/tool effect, file disclosure, plus explicit irreversible dispositions. AI context remains blocked until every item is acknowledged or permissibly dispositioned. Prevents state-token drift and incomplete correction completion.
- (decision) Amend AD-12: A5/A6/A13 block M0 and are revalidated later; exact A9a detector/classifier qualification blocks first use and is revalidated at M1/M2; A11-M1 independently blocks M1 with the frozen SM8, SM16, SM-C3, and SM-C5 measurement contract plus SM12/SM15 counts; M2 revalidates lower gates and additionally requires A10 and A11-M2 exact-candidate SLO qualification. Prevents an M2-only A11 interpretation and one gate result authorizing another.
- (direction) Update _bmad-output/planning-artifacts/architecture.md and architecture/architecture-chatbot-2026-09-14/ARCHITECTURE-SPINE.md atomically; preserve existing AD IDs and add no extra audience-facing deliverable.
- (decision) Clarify amended AD-7 against the normative PRD/addendum: classifier-indeterminate creates no durable idempotency state of any kind; the only retained record is the separate redacted non-mutating auditable attempt. Prevents a terminal failure marker or replay cache from becoming durable approval/retry authority.
- (constraint) Numeric confidence and threshold-band fields belong to association candidates and task-intent evidence, not the categorical ActionRiskClassifier result; risk results carry class, classifier version, and input tuple and cannot be softened by confidence.
- (decision) Amend AD-2 to bind the PRD/addendum closed command-to-admission-profile map: authentication, tenant, authorization, operation identity, concurrency, and envelope construction are universal; only the centrally selected profile may require risk classification or approval, and unknown/absent/duplicate mappings reject. Prevents universal-versus-profile-specific pipeline divergence.
- (decision) Amend AD-7 to make the ChatBot CommandGateway/auditable-attempt seam owner of classifier-indeterminate attempt identity and successor linkage: the audit attempt records original operation_id and classifier evidence, a new request uses a fresh operation_id with immutable predecessor reference, and the attempt ledger can deny reuse but never authorize continuation or act as domain idempotency. Prevents contradictory retry/link implementations.
- (decision) Amend AD-9 to make the ChatBot correction aggregate sole owner of immutable manifest membership/lifecycle while owner contexts retain effect authority; each item has stable correction/owner/resource-or-effect/version-or-digest/outcome identity, and its A13 mapping names the only authenticated acknowledgement/disposition actor. Prevents dual ownership and incompatible item completion.
- (decision) Amend AD-12 with one Release-Governance-owned immutable gate registry and per-candidate/increment gate_set_id consumed atomically by CI/release and runtime enablement; incomplete or mixed-candidate sets fail closed and local booleans/copies grant no authority. Prevents release/runtime consuming different gate records.
- (version) Official NuGet Gallery reality-check on 2026-09-15 confirms xunit.v3 4.0.1 and xunit.v3.core 4.0.1 exist; the repository remains pinned to xunit.v3 4.0.0 and no upgrade is authorized by this architecture update.
- (event) spine finalized
