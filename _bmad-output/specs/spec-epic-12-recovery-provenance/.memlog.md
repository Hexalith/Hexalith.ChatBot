---
topic: Epic 12 recovery provenance implementation specification
updated: 2026-09-14T11:23
---

- (direction by user) Update the Epic 12 recovery-provenance specification from the final architecture spine; AD-1 through AD-9 are authoritative and their identifiers must remain stable.
- (direction by user) Preserve valid recovery requirements, flag contradictions, update affected story artifacts, validate the result, and do not modify code, CI workflows, repository settings, or the architecture spine.
- (decision) Headless express derivation uses slug epic-12-recovery-provenance; the final architecture spine remains an adopted companion and decision authority.
- (capability) CAP-1 intent: operations can run the mandatory continuity, rebuild, and six scoped-outage scenarios through real sandbox boundaries; success: every scenario produces its existing fail-closed report semantics with non-zero coverage, restoration, isolation, and residuals.
- (capability) CAP-2 intent: an exact protected-main pull-request candidate can produce one current-run recovery-primary completion lane; success: planning authorizes exactly one canonical consumer and any production, timeout, no-test, projection, attestation, validation, or publication failure keeps the check red.
- (capability) CAP-3 intent: reviewers can prove destructive recovery left no run-owned mutation or fault behind; success: the sole finalizer reconciles the complete expected observation set and only a complete identity-bound cleanup receipt can be projected and attested.
- (capability) CAP-4 intent: reviewers can distinguish diagnostics, completion authority, and A10 operational evidence; success: closed disjoint 30-day artifact channels reject cross-use, raw producer TRX remains unretained, and only a passing completion-transition report has completion authority.
- (capability) CAP-5 intent: recovery completion runs inside bounded supported and isolated infrastructure; success: version authorities, immutable action references, absolute deadlines, fresh hosted runners, Testing guards, generated secrets, and no GitHub concurrency group are mechanically enforced.
- (capability) CAP-6 intent: governance can activate or defer the control without overstating evidence; success: TE-2 records every activation prerequisite before Story 12.15 or TE-2 reaches done, while A10 and all named deferred decisions remain separately governed.
- (constraint) AD-1 is stable and authoritative: recovery-primary is current-run only at the exact canonical TRX/provenance paths and locator, with at most one active consumer.
- (constraint) AD-2 is stable and authoritative: repository planning precedes destructive setup and exact-head production orders producer, cleanup, projection, preflight, attestation, validation, and publication fail closed.
- (constraint) AD-3 is stable and authoritative: scheduled/release operational evidence informs only the independent recovery/A10 gate and cannot satisfy recovery-primary.
- (constraint) AD-4 is stable and authoritative: diagnostic and completion authority use closed disjoint artifacts, read-only workflow permissions, metadata minimization, and 30-day retention; raw producer TRX is never uploaded.
- (constraint) AD-5 is stable and authoritative: preserve producer-relative ordering 250<265<285 and absolute ordering 315<330<335<350<355<360, with exclusive publication windows.
- (constraint) AD-6 is stable and authoritative: only the always-run check story-transition-evidence-integrity (pull_request) for PRs targeting protected main may gain completion authority, and activation remains pending until the exact rule/source/run is recorded.
- (constraint) AD-7 is stable and authoritative: versioned cleanup observations and one receipt are exact-set, identity-bound, positive-postcondition inputs; RecoveryCleanupTrxBinding exclusively encodes and validates the three cleanup attributes.
- (constraint) AD-8 is stable and authoritative: global.json, the AppHost SDK/package catalog, Dapr CLI/runtime 1.18.2/1.18.4 with verified digest, and reviewed full artifact-action SHAs own executable versions.
- (constraint) AD-9 is stable and authoritative: destructive recovery runs only on fresh GitHub-hosted job-local ephemeral infrastructure with Testing guards, generated secrets, no production endpoints, and no GitHub concurrency group.
- (decision) Preserve the existing live-driver scenario matrix, verdict semantics, A10-only RPO/RTO scope, topology residuals, and fault-authority boundary; architecture provenance narrows evidence authority without deleting these valid requirements.
- (note) Contradictions resolved in favor of AD-1 through AD-9: old shared/per-commit concurrency wording, raw-TRX upload language, Dapr 1.18.0 and Aspire 13.4.6 pins, older timeout ordering, and historical release evidence as completion authority are superseded.
- (decision) No stories.yaml is produced in headless mode; the existing Epic 12 Story 12.15 catalog entry and implementation story are the affected story artifacts to reconcile.
- (event) Self-validation pass 1 coherence PASS: six stable capabilities each have intent and testable success; AD-1 through AD-9 are unique and stable; constraints, non-goals, success signal, contradiction handling, and activation-pending truth are coherent and lean.
- (note) Wrapper-only content not duplicated: historical review chronology, superseded implementation narration, and architecture-production ceremony remain in their owning artifacts; every current load-bearing requirement is preserved in SPEC.md, provenance-contract.md, or the adopted architecture companion.
- (event) Self-validation pass 2 preservation PASS: all AD-1 through AD-9 rules, interfaces, evidence channels, deadlines, cleanup schemas and codec, version authorities, activation requirements, deferred decisions, mandatory recovery scenarios, verdict semantics, A10 boundary, and topology residuals are represented.
- (event) Scope validation PASS: only the spec workspace, Epic 12 catalog, and Story 12.15 implementation artifact changed; code, CI workflows, repository settings, dependencies, and the architecture spine remain unchanged.
- (event) Spec derivation finalized with no assumptions or unresolved open questions; authoritative AD decisions resolve every identified contradiction and activation work remains explicitly pending.
