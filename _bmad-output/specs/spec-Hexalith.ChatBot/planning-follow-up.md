# Planning Reconciliation Follow-up

These open findings were reviewed against the workspace contents on 2026-09-14. They identify delivery artifacts that conflict with or incompletely represent the finalized PRD package and architecture spine. The artifacts are mutable, so each finding must be rechecked before action. This is a discovery ledger, not an execution tracker; the owning workflow must assign an owner, dependencies, acceptance check, and immutable closure evidence before closing an item. This spec-creation run does not edit or reinterpret the cited artifacts.

## Contract reconciliation

| ID | Exact artifact evidence | Conflict or gap | Required follow-up |
| --- | --- | --- | --- |
| PF-1 | `_bmad-output/planning-artifacts/epics.md` §Core architectural decisions D3/D4, Story 1.4, and Story 1.9; `_bmad-output/implementation-artifacts/1-4-fail-closed-audit-commit-seam-with-pre-and-post-commit-audit-emission.md` is `done` | They prescribe pre-commit audit plus fail-open post-commit canonical/WORM emission and reconciliation. PRD FR81a/NFR15a and AD-2/AD-10 require the domain event, lifetime terminal idempotency result, policy/approval references, and canonical envelope to co-commit, or nothing commits. Post-commit views cannot restore the required atomic boundary. | Rebaseline the epic and affected stories to the atomic boundary, identify obsolete code/evidence, and reassess completion without treating the current `done` status as A13 or tamper-evidence proof. |
| PF-2 | `_bmad-output/planning-artifacts/epics.md` FR74 inventory, Epic 9 Story 9.6, legacy Stories 7.24–7.26; `_bmad-output/implementation-artifacts/7-24-disable-outbound-channel.md`, `7-25-quarantine-outbound-channel.md`, and `7-26-rate-limit-outbound-channel.md` | They model outbound channel as a fifth safety-control subject with separate command/state vocabulary. The final PRD closes FR74 to exactly four subjects and treats outbound as a command capability. | Fold outbound enforcement into `command-capability`, reconcile or retire the extra public commands, states, messages, tests, and status aliases, and preserve historical evidence without retaining a fifth subject. |
| PF-3 | `_bmad-output/planning-artifacts/epics.md` platform stack and implementation sequence | The catalog still cites SDK `10.0.302`, Dapr `1.17.x`, Aspire `13.3.x`, xUnit `3.2.x`, seven sibling contexts, a custom pipeline split, and older recovery sequencing. The final spine and `architecture.md` define the current nine-context baseline, version planes, AD-1–AD-19, and blocked qualification posture. | Refresh the epic-level architecture context from the current spine; do not infer dependency upgrades, support, or qualification from the refreshed pins. |
| PF-4 | `_bmad-output/planning-artifacts/epics.md` open-assumption list and absence of the stable bootstrap command IDs | The list stops at A11, omits A12/A13, and does not carry the final command-created M0 governance bootstrap or exact admin/policy/service-client command catalog. | Add explicit A12/A13 treatment and plan AD-6 stories/acceptance for two current Tenants owners, the first immutable ChatBot grant, M0 policy rows, four M0 client grants, separation of duty, owner acceptance, and no direct seed. |
| PF-5 | `_bmad-output/planning-artifacts/epics.md` and existing story acceptance contain no exact `Retry Profile v1` registry mapping | Generic retry/idempotency stories do not preserve all 12 product-authoritative rows, family-specific terminality, maxima, full-jitter caps, exhaustion owner, manual command, and successor form. | Map every affected workflow/story to one addendum retry row and add conformance coverage; do not broaden retry while doing so. |

## Evidence and tracking reconciliation

| ID | Exact artifact evidence | Conflict or gap | Required follow-up |
| --- | --- | --- | --- |
| PF-6 | `_bmad-output/implementation-artifacts/sprint-status.yaml` marks Story 1.5, 4.7, Epic 10 lifecycle/allowlist, and many M0/M1 epics `done`; `reconcile-full-sibling-a13-2026-09-14.md` says Conversations has no production append handler or accepted lifetime idempotency/concurrency/atomic-audit mapping | Historical/local story completion can be real while the owner-executable product contract remains unimplemented or unaccepted. The tracker does not make that scope distinction visible. | Revalidate affected `done` entries against A13 and annotate or reopen them as policy requires; keep implementation completion separate from owner acceptance, pilot qualification, and increment readiness. |
| PF-7 | `_bmad-output/implementation-artifacts/sprint-status.yaml` marks the SLO publication/error-budget story and Epic 11 `done`; every row in `qualification-evidence.md` is `unsupported` with `not-selected` candidate | Readers can misinterpret `done` as proof of A11 qualification or of published, supported SLOs, even if a dashboard or code path displays `unsupported`. | Bind story acceptance and status to the full A11 evidence contract, or explicitly scope completion to unsupported-state rendering; add an explicit A11 open-gate record to sprint reporting. |
| PF-8 | `_bmad-output/implementation-artifacts/sprint-status.yaml` marks continuity and recovery Stories 12.11–12.14 `done`, Story 12.15 `review`, and Epic 12 `in-progress`; A10 is open and the Epic 12 evidence contract is `activation: pending` | Historical harness completion and story evidence cannot satisfy current A10; the 2026-08-27 bundle is expired and non-qualifying. | Keep story completion, activation, exact-candidate completion evidence, and A10 operational qualification as four distinct tracked states; require fresh controlled-loss/RTO-capable evidence before any M2 claim. |
| PF-9 | `_bmad-output/implementation-artifacts/sprint-status.yaml` uses canonical keys whose implementation files remain under legacy numbering; `_bmad-output/planning-artifacts/epics.md` carries a prose legacy-to-canonical map | Consumers cannot reliably use a status key to locate a single canonical artifact, and they can incorrectly attribute historical `done` evidence to a changed canonical scope. | Add a machine-readable canonical-to-legacy artifact/evidence map or create canonical story artifacts, then validate every status key against its exact file, acceptance scope, and evidence digest. |
| PF-10 | `_bmad-output/implementation-artifacts/sprint-status.yaml` has no top-level readiness ledger for A5, A6, A10, A11, and A13 | Numerous epic/story `done` entries can be aggregated into a false readiness signal even though all five release gates remain open. | Add explicit gate state, evidence locator, approver, candidate, expiry/reopen rule, disable/rollback condition, and permitted-claim fields sourced from the PRD qualification artifacts; never derive release readiness from story counts. |

## Authority-maintenance findings

The reviewer gate found the following conflicts or underspecified edges inside the adopted authorities. The SPEC does not choose between them. Their owning PRD or architecture workflow must resolve them while preserving product-authority precedence and stable AD identifiers.

| ID | Exact authority evidence | Conflict or gap | Required owner follow-up |
| --- | --- | --- | --- |
| AF-1 | Approved addendum §Retry Profiles; final architecture spine AD-4 | The addendum contains 12 workflow-family rows, while AD-4 calls it an eleven-row registry. | Correct the spine's count while preserving AD-4's identifier, title, and substantive rule; validate all 12 rows against implementation and tests. |
| AF-2 | Final PRD FR74 and addendum `safety.controls`; final architecture spine AD-19 | Product authority permits exactly four safety-control subjects and treats outbound as `command-capability`. AD-19 separately lists “outbound channels” in its combined control/rate-limit projection, which can be read as a fifth safety-control dimension. | Clarify AD-19's safety-control versus operational-limit wording without changing its identifier or its durable/fail-closed/fairness rules; outbound admission must consume product-authoritative command-capability control. |
| AF-3 | Approved addendum §Tenant Policy Schema and §Shared Command Pipeline; PRD FR55/NFR15a | The schema says every rejected policy change produces a canonical audit envelope, while product authority reserves canonical mutation envelopes for committed mutations and puts sensitive non-mutations on a separate auditable-attempt path. | Name the rejected-policy record type and durability path explicitly; preserve the prior policy snapshot and never let a rejected non-mutation masquerade as canonical mutation completeness. |
| AF-4 | Final PRD §Shared Workflow Contract lifecycle line and authoritative association matrix | The summary permits `Deferred → NeedsReview` only, while the matrix also permits `ConfirmEmailProjectAssociation` and `RejectEmailProjectAssociation` directly from `Deferred`. | Reconcile the summary and matrix through the PRD workflow; do not invent a transition locally. |
| AF-5 | Final PRD FR28b/FR28e and the governed-chat-attempt row | FR28b exposes `needs-review`, but the workflow row has no corresponding state/event. FR28e requires risky requests to become proposals, while `ApprovalRequired` has no explicit atomic link to exactly one `ProposeAIAction` successor. | Complete the governed-chat state/event/successor contract and its retry/status mapping in the PRD before implementation relies on either edge. |
| AF-6 | Final PRD emergency-safety-control row | `ApplySafetyControl` is defined from `Active`, and `ReleaseSafetyControl` returns any controlled subject to `Active`; no direct controlled-to-stricter successor is defined. | Decide whether direct tightening is required and, if so, add its command guard, immutable successor transition, event, and race behavior without a permissive intermediate state. |
| AF-7 | Final PRD FR34 and attachment-handling row | FR34 names `captured`, `pending`, `unavailable`, `rejected`, `unsafe`, `failed`, and `retryable`; the lifecycle row uses only `PendingScan`, `Stored`, `Unsafe`, and `Failed` plus a linked retry attempt. | Publish the exact state/event/typed-flag mapping so surfaces and tests do not invent aliases or omit required outcomes. |
| AF-8 | `architecture.md` §Format Patterns “Evidence & confidence capture”; approved addendum §Risk Classifier | The architecture requires a numeric `confidenceScore` for every proposal/candidate, while the product-authoritative action-risk classifier is categorical and explicitly emits no numeric confidence score. | Limit the numeric shape to scored records or define a non-risk source for proposal confidence without weakening the categorical risk contract. |

## Reconciliation rule

For each follow-up, first compare the current artifact with the PRD package and then with the architecture spine. Record the conflict instead of choosing silently. Update the owning artifact through its normal workflow, and rerun the contract/source, lifecycle/retry, and sprint-status integrity checks. No finding closes or changes a release gate.
