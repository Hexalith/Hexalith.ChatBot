# Test Automation Summary — Story 7.14 (Rate-limit mailbox source)

_Generated by the `bmad-qa-generate-e2e-tests` workflow, 2026-06-02. Engineer: Jerome (QA automation). Framework: .NET `net10.0` / xUnit v3 + Shouldly (repo-pinned; no new framework introduced), compiled in-process runners (`-parallel none`)._

## Scope

Story 7.14 is a server/command feature (single-actor `SubmitMailboxSourceRateLimit` mutation + worker enforcement). It has **no UI surface** — the S5 admin/queue surface was deliberately deferred, consistent with 7.12/7.13 — so no Playwright/browser E2E layer applies. "E2E" coverage here is the gateway → aggregate → worker / audit integration the project already uses. This QA pass mapped existing coverage to the 10 acceptance criteria, found three genuine gaps, and auto-applied focused tests for each.

## Generated / Extended Tests

### Worker enforcement (new) — `tests/Hexalith.ChatBot.Workers.Tests/Mailbox/GraphMailboxIntakeWorkerTests.cs`
- [x] `ControlStateBlockShouldTakePrecedenceOverRateLimitForNonActiveSource` (Theory: Disabled, Quarantined) — proves the rate-limit branch sits **after** the disable/quarantine control-state blocks and applies **only to an Active source**: a blocked source whose window is already at budget returns its control-state await-admin reason, never the rate-limit defer, never fetches, and carries **no** rate-limit observation. **(AC5)**
- [x] `RateLimitCounterShouldBeIndependentPerTenantForTheSameMailboxSource` — proves the trailing-window counter is keyed per `(tenant × mailbox source)`: tenant-alpha at budget defers while tenant-beta (same mailbox source id, own empty counter) processes normally. Deferring a noisy tenant never throttles another tenant. **(AC6, NFR30/NFR18 — the "another tenant" isolation clause)**

### Gateway audit (extended) — `tests/Hexalith.ChatBot.Server.Tests/Gateway/CommandGatewayTests.cs`
- [x] `MailboxSourceRateLimitAuditEnvelopeShouldCarryBudgetWindowAndRemainMetadataOnly` — added an assertion for the `mailbox-source-rate-limit-source-version:4` audit ref, so the recorded old/new budget ("old state"/"new state") is anchored to a source version and the mutation is fully reconstructable from metadata alone. **(AC4)**

## Coverage — Acceptance Criteria

| AC | Description | Status |
|----|-------------|--------|
| 1 | Single-actor durable budget recorded + applied | ✅ existing (auth + aggregate + worker) |
| 2 | Bounded by schema; out-of-range rejected; safe-default fallback | ✅ existing (auth + contract + worker fallback) |
| 3 | Human mailbox/tenant-admin only; service/AI denied | ✅ existing (authorization tests) |
| 4 | Audit fields (incl. source-version) + fail-closed pre-commit | ✅ existing + **gap filled** (source-version ref) |
| 5 | Defer-before-fetch, **after disable/quarantine, Active-only** | ✅ existing defer + **gap filled** (precedence) |
| 6 | Queue-impact metrics + isolation | ✅ existing sibling + **gap filled** (cross-tenant) |
| 7 | Safe transient catalog guidance (retry-later/dependency-degraded) | ✅ existing (catalog tests) |
| 8 | Safe-token-only serialization | ✅ existing (contract + gateway redaction) |
| 9 | OpenAPI-first → client regen → checksum → parity | ✅ existing (client parity tests) |
| 10 | Acceptance rollup | ✅ all of the above |

## Validation Results

Build: `dotnet build Hexalith.ChatBot.slnx --no-restore -m:1 /nr:false` → **0 Warnings, 0 Errors**.

Compiled in-process xUnit v3 runners (`-parallel none`):

| Suite | Total | Failed | Note |
|-------|-------|--------|------|
| Hexalith.ChatBot.Workers.Tests | **30** | 0 | +3 vs baseline (27) — the new worker tests |
| Hexalith.ChatBot.Server.Tests | **708** | 0 | unchanged count (extended an existing test) |
| Hexalith.ChatBot.Contracts.Tests | 254 | 0 | baseline confirmed |

**Expected: all tests pass ✅ — achieved.**

## Checklist (`checklist.md`)

- [x] API/command tests generated (worker + audit seams; no new endpoints)
- [N/A] E2E/UI tests — no UI surface (deferred per story)
- [x] Standard framework APIs (xUnit v3 + Shouldly)
- [x] Happy path covered (under-budget processes; cross-tenant beta processes)
- [x] Critical error/edge cases covered (defer-at-budget, control-state precedence, cross-tenant isolation)
- [x] All generated tests run successfully
- [x] Clear, descriptive test names
- [x] No hardcoded waits/sleeps (injected fake clock + fake intake history — deterministic windows)
- [x] Tests independent (no order dependency)
- [x] Summary created with coverage metrics

## Notes

- No production code was changed — QA test-generation only. No submodule pointers touched.
- Redaction is re-asserted on the new precedence test (`ToString()` carries no `@`), consistent with the disable/quarantine precedents.

## Next Steps

- Run these suites in CI alongside the existing Client/Conformance/Architecture regression set.
- When the durable read-side projection (deferred per story) lands, add an integration test that wires `IMailboxConfigurationProvider` to observe `MailboxSourceRateLimitConfigured` and maintain durable intake history end-to-end.
